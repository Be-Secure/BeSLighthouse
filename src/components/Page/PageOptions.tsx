
import * as React from "react";

function PageOptions({ children }: any): any {
  return <div className="page-options d-flex">{children}</div>;
}

PageOptions.displayName = "Page.Options";

export default PageOptions;
