
import * as React from "react";

function PageSubTitle({ children }: any): any {
  return <div className="page-subtitle">{children}</div>;
}

PageSubTitle.displayName = "Page.SubTitle";

export default PageSubTitle;
