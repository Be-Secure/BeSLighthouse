import * as React from "react";

function PageMapHeader({ children }: any): any {
  return <div className="map-header">{children}</div>;
}

PageMapHeader.displayName = "Page.MapHeader";

export default PageMapHeader;
